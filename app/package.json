{
  "name": "akilas-archive-app",
  "version": "1.0.0",
  "private": true,
  "description": "Akilas Archive — public terminal UI + admin panel serving Cloudflare R2 buckets",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js"
  },
  "dependencies": {
    "aws4fetch": "^1.0.20",
    "better-sqlite3": "^12.2.0",
    "express": "^5.1.0"
  }
}
